<html>
  <head>
    <title>Email Verification</title>
  </head>
  <script>
    const baseUrl = process.env.NEXT_PUBLIC_BASE_URL_FE; 
    const verifyLink =`${baseUrl}/events`; 
    ke DOM document.write(`<a href="${verifyLink}">Verify
    Email</a>`);
  </script>
  <body>
    <h1>Verify Your Email</h1>
    <p>Click the link below to verify your email:</p>
    <a href="{{verifyLink}}" }>Verify Email</a>

  </body>
</html>